# 🌐 WiFi QR Share (Делимся WiFi через QR)

Минималистичная, безопасная и работающая оффлайн утилита на Python для создания QR-кодов подключения к WiFi. Разработана с использованием стандартной библиотеки Python (tkinter) и Segno.

## 🚀 Возможности

- **Мгновенная генерация**: Создание QR-кодов для сетей WPA/WPA2, WEP и открытых сетей.
- **Конфиденциальность**: Работает на 100% локально. Никакие данные не отправляются в облако. Пароли нигде не сохраняются.
- **Удобная печать**: Генерация HTML-страниц формата A4 с инструкцией "Сканируйте для подключения".
- **Безопасность**: Корректная обработка специальных символов в SSID и паролях с использованием стандартных правил экранирования.
- **Скрытый ввод**: Поле ввода пароля скрыто звездочками для безопасности при демонстрации.
- **Скрытые сети**: Поддержка настройки для скрытых WiFi сетей.
- **Десктопный GUI**: Нативное десктопное приложение с использованием встроенной библиотеки tkinter.

## 🛠️ Технологический стек

- **Python 3.10+**
- **tkinter**: Встроенный GUI-фреймворк (не требует дополнительной установки).
- **Segno**: Для генерации QR-кодов в соответствии со стандартами.
- **Pillow**: Для обработки изображений.

## 📦 Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/galimov-i/wifi-qr-share.git
   cd wifi-qr-share
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

   Примечание: `tkinter` обычно входит в стандартную установку Python. Если его нет, установите через менеджер пакетов вашей системы:
   - **Ubuntu/Debian**: `sudo apt-get install python3-tk`
   - **macOS**: Обычно предустановлен вместе с Python
   - **Windows**: Входит в стандартную установку Python

## 🖥️ Использование

1. Запустите приложение:
   ```bash
   python app.py
   ```

2. Введите данные вашей WiFi сети в графическом интерфейсе:
   - **Network Name (SSID)**: Введите имя вашей WiFi сети.
   - **Password**: Введите пароль сети (необязательно для открытых сетей).
   - **Security Type**: Выберите тип защиты (WPA, WEP или nopass) из выпадающего списка.
   - **Hidden Network**: Отметьте галочку, если ваша сеть не транслирует свое имя.

3. QR-код будет сгенерирован автоматически по мере ввода данных.

4. **Действия**:
   - **Download PNG**: Сохранить QR-код как PNG изображение.
   - **Print Friendly HTML**: Сохранить готовую к печати HTML-страницу формата A4.

## 🔒 Примечание о безопасности

Этот инструмент работает полностью на вашем локальном компьютере. Он не хранит, не логгирует и не передает ваши учетные данные WiFi. Генерация QR-кода происходит в оперативной памяти.

## 📝 Лицензия

[MIT](LICENSE)
